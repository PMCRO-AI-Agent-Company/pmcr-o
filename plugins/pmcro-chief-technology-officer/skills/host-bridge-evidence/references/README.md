# References -- pmcro-chief-technology-officer:host-bridge-evidence

## Asset map

| Asset | Purpose |
|---|---|
| `assets/setup.host-bridge-evidence.asset.md` | the real device-link/folder-access/local-MCP-server sequence, and how a mid-session drop was actually recovered from |
| `assets/tools.host-bridge-evidence.asset.md` | the call catalog, grouped by evidence use |
| `assets/recipe.host-bridge-evidence.asset.md` | how Maker/Checker fold bridge output into trail evidence |

## Source and history

Built directly from one cloud-hosted Claude session's live use against
this repository on 2026-09-04 -- device-info query, a real folder-access
grant for this repo's own root, a real local-MCP-server ("Desktop
Commander") read of this repo's governance files, a real mid-session
disconnect, and a real recovery (device-info re-check plus an explicit
tool-list refresh, no user action needed). Unlike `pmcro-aspire`'s first
draft, this skill's v0.1.0 is written from what was actually exercised,
not from documentation read in advance of running it -- there is
accordingly no "corrected in trail X" history yet, because there is only
one real trail behind it so far. Update these assets, not this note,
the next time real use disagrees with what is written here.
