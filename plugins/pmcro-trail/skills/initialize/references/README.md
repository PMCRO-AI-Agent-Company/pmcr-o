# References — pmcro-trail:initialize

## Asset map

| Asset | Purpose |
|---|---|
| `assets/command.initialize.asset.md` | invocation, parameters, result/error shape |
| `assets/run.initialize.asset.md` | accept-path implementation (mint or link) |
| `assets/reject.initialize.asset.md` | refusal-path implementation |
| `assets/schema.trail-frame.asset.json` | the frame contract every `*.jsonl` line in a trail must satisfy |
| `scripts/New-Trail.ps1` | deterministic mint-path implementation (zero reasoning) |
| `scripts/Test-TrailLink.ps1` | deterministic link-path implementation (zero reasoning, read-only) |

## Source URLs

None yet — this plugin was authored fresh in this repo, not ported from an
external source. When content is later ported from elsewhere, its origin
URL is recorded here, not inline in `SKILL.md` or the assets above.
